Good pairing — Danilov et al. is essentially a solved instance of your prompt's structure (ADM + LDF + nonlinear isotherm + two-phase heat balance), minus the wall term and with Sips instead of Toth. Here's the usage map, plus some errata you need to know before transplanting anything.

## 1. What transfers directly into Part A

**Variable dictionary.** They work in mole fraction $y$ and molar flux $F$; you work in concentration. For dilute ideal gas, $c = \rho_{mol}^G y$ with $\rho_{mol}^G = P/RT$, and their $(1-\varepsilon_b)\rho_b$ source prefactor is your $(1-\varepsilon)\rho_p$ (careful: they call $\rho_b = 588.5$ kg/m³ "bulk density" while also carrying $(1-\varepsilon_b)$ — that double-counts unless $\rho_b$ is actually particle density; your prompt's $(1-\varepsilon)\rho_p$ convention is the clean one, keep it).

**The flux recasting is the piece worth stealing.** Their A.3–A.4 define

$$F = uc - \varepsilon D_L \frac{\partial c}{\partial z}, \qquad \varepsilon\frac{\partial c}{\partial t} = -\frac{\partial F}{\partial z} - (1-\varepsilon)\rho_p\frac{\partial q}{\partial t}$$

This does three jobs for your prompt simultaneously: (i) it makes the conservation check trivial — integrate over the bed and you get $\frac{d}{dt}\int_0^L[\varepsilon c + (1-\varepsilon)\rho_p q]\,dz = uc_f - F(L,t)$, which is exactly the identity you're asked to prove and the discrete invariant your FV scheme should preserve; (ii) their inlet condition A.9, $F(0,t) = uc_f$, *is* the Danckwerts inlet BC, so the paper hands you the BC treatment; (iii) it's what makes their quadrature solution possible.

**Energy balance derivation route.** Your prompt asks for the pseudo-homogeneous single-$T$ balance. Derive it *from* their two-temperature system (Table 1 gas balance + A.13 solid balance): sum the two, the $\alpha_v(T^S - T^G)$ terms cancel, and you land exactly on

$$C_h\frac{\partial T}{\partial t} + u\rho_g c_{p,g}\frac{\partial T}{\partial z} = \lambda_{eff}\frac{\partial^2 T}{\partial z^2} + (1-\varepsilon)\rho_p(-\Delta H)\frac{\partial q}{\partial t}$$

with $C_h = \varepsilon\rho_g c_{p,g} + (1-\varepsilon)\rho_p c_{p,s}$. The justification for lumping is their Eq. (4)/(A.15): the interphase gap is $\Delta T = (1-\varepsilon)\rho_p(-\Delta H)\,k\psi/\alpha_v$, so you get a quantitative a-priori criterion — pseudo-homogeneous is valid when $(1-\varepsilon)\rho_p(-\Delta H)k\,q_f/\alpha_v \ll \Delta T_{ad}$. That's a stronger justification than the usual hand-wave. The wall-loss term $4h_w(T-T_{wall})/d_{col}$ is absent in the paper (their column is effectively adiabatic); it just adds as a sink in the lumped balance, or as an extra term in the integrand of their heat-flux quadrature (3) if you follow their route.

**Isotherm swap is trivial.** Their framework is isotherm-agnostic — the isotherm enters only through pointwise evaluation of $q^*(c,T)$ in Eq. (10) and through $\partial q^*/\partial c$ inside $\gamma_q$. Replace Sips with Toth: $\partial q^*/\partial c = n_s b\left[1+(bc)^{t_T}\right]^{-(1+1/t_T)}$. Nothing structural changes.

## 2. The core asset: their ψ solution is your "analytically tractable limiting case"

The genuinely novel move in the paper (Appendix B) is collapsing the coupled system via the chain-rule substitutions $\partial y/\partial t = (\partial y/\partial q)(\partial q/\partial t)$ into a scalar ODE for the driving force $\psi = q^* - q$:

$$\frac{\partial\psi}{\partial t} = -\gamma_\psi k\,\psi \;\Rightarrow\; \psi \propto e^{-kt}$$

then stitching a **symmetric front ansatz** around the stoichiometric time (their Eq. 1), from which every field — $F$, $c$, $Q$, $T_G$, $q$, $T_S$ — follows by marching quadrature (Eqs. 2–8), no iteration, no PDE solve. Present this in your solution as the **constant-pattern / travelling-wave limit** of the full model, alongside (i) the linear-isotherm exact solution (their references Rasmuson–Neretnieks 1980 and Liao–Shiau 2000 are the canonical sources) and (ii) the $k\to\infty$ equilibrium/shock limit (De Vault 1943, Tondeur 1987 — also in their bibliography; the reference list is basically a curated map of your limiting cases). You'll need the front kinematics they use but don't spell out: for a clean bed, $t_s = \frac{\varepsilon L}{u}(1+\Lambda)$, $u_f = L/t_s$, with $\Lambda = \frac{(1-\varepsilon)\rho_p q^*(c_f,T)}{\varepsilon c_f}$.

There's also a payoff specific to your existing pipeline: the symmetric double-exponential front is, to leading order, a logistic — i.e., the Thomas/Yoon–Nelson shape your ultracode already fits. The Danilov derivation is the mechanistic bridge: constant-pattern LDF ⇒ symmetric exponential sigmoid, with the fitted Thomas rate ≈ $\gamma_\psi k$ and Yoon–Nelson $\tau \approx t_s$. So your fitted parameters from the PEI@SiO₂ breakthrough data become direct seeds for $k$ in the mechanistic model, and Part A stops being disconnected from your empirical work.

The $\gamma \approx 1$ assumption is safe for you: the correction is $O(1/\Lambda)$, and for amine sorbents $\Lambda \sim 10^2$–$10^3$ (e.g. $q_f \approx 2$ mol/kg, $c_f \approx 4$ mol/m³ at 10% CO₂ gives $\Lambda \sim 500$), so sub-1% error.

## 3. Errata and traps — read before implementing

- **Sign inconsistency in the accepted manuscript.** As printed, A.1/A.4/A.8 have the adsorption source entering the gas balance with $+$, which would make flux *grow* along the bed during adsorption; main-text Eq. (2) and A.18 have the physically correct $-$. Trust Eq. (2)/(A.18) and your own prompt's form. Derive from your control volume, don't transplant.
- **The $\gamma_q,\gamma_\psi$ algebra as printed doesn't compose.** Substituting the nomenclature definition of $\gamma_q$ into $\gamma_\psi$ collapses to 0, not the claimed $\approx 1$. The final journal version may have fixed it; either rederive the correction yourself or set both to 1 (justified above for your $\Lambda$).
- **The symmetric front is an ansatz, not a result.** It presumes a fully developed constant pattern, so it can't capture front formation near $z=0$, and it will misrepresent tailing — their own Fig. 1 shows the analytical curve less tailed than both COMSOL and experiment. For PEI@SiO₂ specifically, chemisorption heat effects and slow amine kinetics tend to produce asymmetric fronts; treat their solution as a limiting case and a benchmark, not the production model. Same caveat for constant $k$: amine uptake is closer to reaction-limited with $k(T) = k_0 e^{-E_a/RT}$.
- **Their concentration reconstruction (Eq. 5/A.20) integrates a growing exponential** $e^{+Pe\,z/L}$ forward from the inlet. Fine at $Pe=10$ on their grid; ill-conditioned for $Pe \gtrsim 50$. At high $Pe$ use the plug-flow closed form $c = F/u$, or solve the two-point BVP properly.
- Constant $u$ and trace-component assumptions are baked in — fine for your inert-carrier prompt, but state them.
- One typo in **your own prompt**: the wall term denominator reads `d_{\mathrm{cfile:///C:/Users/User/...ol}}` — a file path got pasted into the LaTeX. Fix to $d_{col}$ before sending it anywhere.

## 4. Suggested execution order

Derive Part A from the control volume in flux form (paper as template, correct sign), do the two-T → one-T lumping with the A.15 error bound, and add the wall sink. Nondimensionalise yourself — the paper only gives $Pe_G, Pe_T$; you'll want the full set: $Pe$, $Pe_T$, transfer-unit number $N = k\varepsilon L/u$, capacity ratio $\Lambda$, thermal-to-mass front speed ratio $\omega = (1+\Lambda)\varepsilon\rho_g c_{p,g}/C_h$ (decides whether the thermal wave leads or lags the mass front — the paper never addresses this regime split; Ruthven–Garg–Crawford 1975 from their references does), adiabatic rise $\Delta T_{ad} = (1-\varepsilon)\rho_p q_f(-\Delta H)/C_h$, isotherm sensitivity $(-\Delta U_b/RT_f)(\Delta T_{ad}/T_f)$, and wall Stanton number. For the full nonlinear solve, do conservative finite-volume method-of-lines with a stiff integrator and Danckwerts BCs — then use the paper twice for V&V: reproduce their Fig. 1–2 from the complete, self-consistent Table 2 parameter set (that table is rare and worth more than the algorithm), and check your solver collapses onto their ψ-quadrature in the dilute constant-pattern limit before switching the isotherm to Toth.

Want me to implement the ψ-quadrature (Eqs. 1–9, Table 2) as the verification script, or start on the nondimensionalisation and limiting-case proofs?

---

## 5. Verification of §3's errata against the source (spot-check, this session)

Read Appendix A (`danilov2019.txt:1335–1489`) and Appendix B (`:1765–1919`) directly, since §3's claims rest on those two appendices and hadn't been checked against the raw text yet.

- **Front kinematics ($t_s=\tfrac{\varepsilon L}{u}(1+\Lambda)$, §2) — confirmed independently.** Expanding MM's own $t_{st}=L[\varepsilon c_f+\alpha_bq_f]/(uc_f)=(\varepsilon L/u)\bigl[1+\alpha_bq_f/(\varepsilon c_f)\bigr]$ and comparing term-by-term to $\Lambda=(1-\varepsilon)\rho_pq^*(c_f,T)/(\varepsilon c_f)=\alpha_bq_f/(\varepsilon c_f)$ (MM's own $\alpha$, MM §C.2) shows the two are algebraically identical. This is a genuine, useful cross-check — it means Danilov's $\Lambda$ *is* MM's dimensionless capacity ratio $\alpha$ under a different name, not a new quantity to re-derive.
- **$\gamma_q^{(m)}\approx1$ (§1, §2) — confirmed as printed.** Appendix A (`:1466–1478`) defines $\gamma_q^{(m)}$ and states verbatim: "the evaluation of the coefficient indicates that $\gamma_q^{(m)}\approx1$ for the system in Table 2... valid for Langmuir type system adsorption." Appendix B (`:1907–1917`) introduces a *second*, related coefficient (printed as $\beta^{(m)}$, built from $\gamma_q^{(m)}-1$) and states "$\gamma_q^{(m)}\approx1$ and $\beta^{(m)}\approx1$ is valid for a Langmuir type system in Table 2." The derivation route — mass balance in flux form (A.1–A.4) → chain-rule substitution $\partial y/\partial t=(\partial y/\partial q)(\partial q/\partial t)$ (A.5) → single ODE for the driving force $\partial(q_{eq}-q)/\partial t=-\beta k_f(q_{eq}-q)$ (B.5) — matches §2's description exactly.
- **Notation trap found by this check, not previously flagged: §2's "$\gamma$", §1/§3's "$\gamma_q$", and Appendix B's "$\beta$" are three related but distinct symbols, and §2's closing claim ("The $\gamma\approx1$ assumption is safe... correction is $O(1/\Lambda)$") is *not* obviously the same statement as Appendix A's "$\gamma_q^{(m)}\approx1$."** §2's $\gamma$ (no subscript) is the OCR text's shape/symmetry parameter for the front profile itself (`danilov2019.txt:475`, "$\gamma=\gamma(t)$ at high concentrations of the adsorbed component" — a property of the *ansatz shape*), whereas Appendix A's $\gamma_q^{(m)}$ and Appendix B's $\beta^{(m)}$ are *algebraic coefficients* inside the driving-force ODE derivation, unrelated in definition to the front-shape parameter even though both are asserted "$\approx1$" for the same Langmuir/Table-2 system. **Do not conflate these when implementing** — keep three separate names (`gamma_shape`, `gamma_q`, `beta_psi`, say) even though the paper's own inconsistent typesetting invites merging them.
- **§3's sign-inconsistency and "$\gamma_q,\gamma_\psi$ algebra collapses to 0" claims — not independently reproducible from the OCR text.** The `+`/`-` operators in Eqs. A.1–A.8 and B.1–B.5 are frequently dropped by the OCR (rendered as bare whitespace between terms, e.g. `:1391–1398`), so the specific sign error and the specific algebraic collapse-to-zero claimed in §3 cannot be re-derived from `danilov2019.txt` as it currently exists in the repo. This is not a refutation — it is exactly the OCR-unreliability situation §3 itself should be read under. **Treat §3's two algebra-specific bullets ("sign inconsistency," "$\gamma_q,\gamma_\psi$ doesn't compose") as unverified-but-plausible pending the original PDF; treat the $\gamma_q\approx1$/$\beta\approx1$ Langmuir-validity claim and the $t_s,\Lambda$ front-kinematics formula as independently confirmed.**
- **§3's "typo in your own prompt" ($d_{\mathrm{col}}$ corrupted by a pasted file path) — stale, already resolved.** A repo-wide search for `file:///` and the corrupted token turns up nothing outside this document (i.e. only inside this quoted errata bullet itself). `src/docs/prompts/mathematical_model.md`, the "your own prompt" referred to, currently reads `d_{\mathrm{col}}` cleanly throughout (e.g. its Part A energy-balance derivation and worked solution). No action needed; kept below for history but no longer an open item.
- **§4's $N=k\varepsilon L/u$ vs. MM's $Da=kL/u$ (`mechanistic-model.md` §C.2) — same physical ratio, different normalisation, real collision risk.** $N=\varepsilon\,Da$. Both measure "LDF relaxation rate vs. residence time"; neither is wrong, but a script that pulls $Da$ from MM and $N$ from this document's §4 without converting will be off by a factor of $\varepsilon$ (roughly 0.4–0.5 for this rig) in exactly the kind of silent way MM's own §0.1 convention warning already warns about for $u$ and $D_L$. **Standardise on MM's $Da=kL/u$ inside the verification script; convert on the way in if a Danilov-side formula needs $N$.**

---

# PART 1 — IMPLEMENTATION PLAN PROMPT (send as-is to a coding agent with repo file access)

Do not search the internet. Build a verification script, `src/solver/psi_quadrature_verify.py`, that implements Danilov et al. (2019)'s ψ-quadrature — the driving-force ODE $\partial\psi/\partial t=-\beta k_f\psi$ ($\psi=q^*-q$, derived in their Appendix B) stitched into a symmetric front ansatz around the stoichiometric time (their Eq. 1), then marched through the coupled mass/heat balance by the quadratures of their Eqs. 2–8, closed by Eq. 10 — and presents it explicitly as **the constant-pattern / travelling-wave limit of the full mechanistic model already derived in `src/docs/mechanistic-model.md` Part D.3** (delimited below; canonical location for redundancy: https://github.com/lohjo/ProjID3/blob/main/src/docs/mechanistic-model.md). The two constructions solve the same limiting problem two different ways — MM D.3 *derives* the wave shape from an ODE with a proven closed form in the Langmuir sub-case (MM D.8); Danilov *assumes* a shape and marches it — and the value of this script is turning that comparison into numbers and pictures rather than prose.

**Everything in §§1–5 above is required background — read it in full, including §5's verification pass, before writing code.** In particular: use $\gamma_q\approx1,\beta\approx1$ only for the Langmuir/Table-2 regime it was validated for (§5, bullet 2); use the $t_s=(\varepsilon L/u)(1+\Lambda)=t_{st}$ identity (§5, bullet 1) to set Danilov's front time from MM's closed form rather than fitting it independently; keep `gamma_shape`, `gamma_q`, `beta_psi` as distinct named quantities (§5, bullet 3); do not trust the printed sign in Eqs. A.1–A.8/B.1–B.5 without re-deriving from the control volume, per §3's own advice; and normalise $Da=kL/u$ throughout, converting away from $N=k\varepsilon L/u$ at the boundary (§5, bullet 5).

### 1.1 Mechanistic-model excerpt (condensed; the full file is the authority)

<mechanistic_model.md>
**Notation (MM §0.1).** $z\in[0,L]$, $t\ge0$; $\varepsilon$ bed voidage; $u$ **superficial** velocity; $c(z,t)$ gas CO₂ concentration [mol m⁻³]; $q(z,t)$ solid loading [mol kg⁻¹]; $q^*(c,T)$ equilibrium loading (Toth, MM Eq. A.3); $\alpha_b=(1-\varepsilon)\rho_p$; $k$ LDF rate constant [s⁻¹]; $c_f,T_f,T_0$ feed/initial states; $q_f=q^*(c_f,T_0)$.

**Full model (MM Eq. A.6).**
$$\varepsilon c_t+uc_z=\varepsilon D_Lc_{zz}-\alpha_bq_t,\qquad q_t=k(q^*(c,T)-q),\qquad C_hT_t+u\rho_gc_{p,g}T_z=\lambda_{\mathrm{eff}}T_{zz}+\alpha_b(-\Delta H)q_t-\tfrac{4h_w}{d_{\mathrm{col}}}(T-T_{\mathrm{wall}}).$$
Clean-bed IC; Danckwerts inlet $uc_f=uc(0^+,t)-\varepsilon D_Lc_z(0^+,t)$; zero-gradient outlet. Danilov's own flux-form recasting ($F=uc-\varepsilon D_Lc_z$, their A.3) makes $F(0,t)=uc_f$ *literally* this Danckwerts condition — the two formalisms share a boundary condition, not just a family resemblance.

**Inventory identity (MM B.1–B.3).** $dM/dt=uc_f-uc(L,t)$ exactly, for *every* $D_L\ge0,k>0$. **Corollary B.1**: $\int_0^\infty(1-c(L,t)/c_f)dt=t_{st}=L[\varepsilon c_f+\alpha_bq_f]/(uc_f)$ — model-free, front's first moment pinned by equilibrium capacity alone.

**Constant-pattern travelling wave (MM D.3 — the section this script tests against).** Isothermal, $D_L=0$, favorable $Q$. Ansatz $c=\varphi(\eta)$, $\eta=z-vt$; mass balance forces $v=v_{RH}=uc_f/[\varepsilon c_f+\alpha_bQ(c_f)]$ and the coherence relation $q=(q_f/c_f)\varphi$. LDF substitution gives $\varphi'(\eta)=-\tfrac{kc_f}{v_{RH}q_f}[Q(\varphi)-\tfrac{q_f}{c_f}\varphi]$, which for Langmuir $Q=q_mbc/(1+bc)$ integrates in closed form:
$$\ln w-(1+bc_f)\ln(1-w)=-\frac{kbc_f}{v_{RH}}(\eta-\eta_0),\qquad w=\varphi/c_f.\tag{MM D.8}$$
Tail e-folding lengths $\ell_+\ne\ell_-$ (asymmetric) unless $bc_f\to0$, where the profile becomes the symmetric logistic $1/(1+e^{-k_{YN}(t-\tau)})$, $k_{YN}=kbc_f$, $\tau=t_{st}$ (MM D.4) — **the same weak-nonlinearity logistic limit §2 of this document reaches independently from the Danilov side** ($k_{YN}\approx\beta k$, $\tau\approx t_s$). Two independent derivations landing on the same limiting shape is a strong consistency anchor; the interesting comparison is therefore precisely *away* from this shared limit, at $bc_f=O(1)$ or larger, where MM D.8 is asymmetric and Danilov's Eq. 1 ansatz is symmetric by construction.

**No closed form once non-isothermal (MM D.6).** $q^*(c,T)$ nonlinear in both fields; Arrhenius $b(T)$ feeds mass$\to$heat$\to$mass; concentration and thermal waves travel at different speeds ($v_{RH}$ vs. $v_{th}=\gamma_hu$), producing plateaus/roll-up (confirmed numerically, MM Appendix V.5: adiabatic run stalls at $c/c_f\approx0.955$ while $T$ is $+18.9\,$K above ambient — a two-front structure no single symmetric-or-asymmetric one-front ansatz, including Danilov's, can represent by construction).

**Existing validated numerics (`mechanistic_verify.py`, MM Appendix V) — the comparator for this script.**

| Test | Claim | Result |
|---|---|---|
| V2 | $v_{RH}$ (D.3) + first moment $t_{st}$ (Cor. B.1) | front-speed error 0.020%, moment error <0.005% |
| V3 | Exact Langmuir travelling wave (D.8) | RMS deviation 0.30%, zero fitted parameters |
| V5 | Full non-isothermal Toth | adiabatic $\Delta T_{\max}=18.9\,$K, documented hot-plateau roll-up |
</mechanistic_model.md>

### 1.2 Task list

1. Create `src/solver/psi_quadrature_verify.py`. Import `Bed`/`Langmuir`/`Toth` from `mechanistic_verify.py` rather than duplicating them, so both scripts share one parameter source of truth.
2. Implement the driving-force ODE and Eqs. 1–8 as a function `psi_quadrature(bed, iso, N, t_eval, gamma_q=1.0, beta_psi=1.0)`, with `gamma_q`/`beta_psi` as explicit keyword arguments defaulting to 1 (valid only in the Langmuir/Table-2 regime per §5) rather than hard-coded — this makes the Langmuir-only validity of the default an explicit, visible choice, not a silent one. Set $t_s\leftarrow t_{st}$ (MM D.4) and $u_f\leftarrow v_{RH}$ (MM D.3) rather than fitting them (§5, bullet 1; erratum in §3 restated: don't refit what MM already derives in closed form).
3. **Reproduction check (do this before any project-specific comparison).** Reproduce Danilov's own Fig. 1 using their Table 2 parameters (Möller et al. 2017, CO₂/CH₄/He on Shirasagi MSC CT, Sips isotherm) at $Pe_G=10$ and $Pe_G\to\infty$ (plug flow). This isolates OCR-reconstruction bugs in Eqs. 1–8 from isotherm/species-mismatch confounds (§1's isotherm swap and Table 2's system are both irrelevant to this specific check) before touching this project's Toth/single-component system at all.
4. **Consistency test vs. MM D.8 (isothermal Langmuir).** Swap in single-component Langmuir, run the ψ-quadrature and compare its outlet trace and 0.65$\,t_{st}$ profile snapshot against (a) MM D.8's exact closed form and (b) `mechanistic_verify.py::test3_wave`'s FV-MOL solution, at the same snapshot time so the three-way comparison is apples-to-apples. Report RMS deviation from both. This is the test that is actually informative *because* the shared weak-nonlinearity limit (§1.1 above) is not being tested here — pick a Langmuir $b$ with $bc_f=O(1)$–$O(10)$ (`mechanistic_verify.py::test3_wave` already uses $bc_f=2.045$; reuse it) so the asymmetric-vs-symmetric distinction is actually visible.
5. **Gate-B-style check.** Compare implied front speed and first-moment time against $v_{RH}$, $t_{st}$, the way `mechanistic_verify.py::test2_rh` does.
6. **Non-isothermal demo.** Toth closure, illustrative parameters matching `mechanistic_verify.py::test5_full`; overlay outlet trace and $T^G$ against MM's V5 (adiabatic + wall-coupled). State explicitly in the printed output that Danilov's own validity restriction (Eq. 1 ansatz assumes low-concentration/dilute, `danilov2019.txt:473–481`) is being exceeded here if the illustrative $c_f$ is not dilute — do not silently apply it outside its stated range.
7. **Figures**, `src/img/generated/psi_quadrature/`: (i) Danilov Fig.-1 reproduction; (ii) three-way Langmuir profile overlay (Task 4), styled like `V3_travelling_wave.png`; (iii) non-isothermal outlet+temperature overlay against `V4_nonisothermal.png`.
8. Printed summary table: RMS deviations, front-speed/$t_{st}$ percent errors, and wall-clock cost of the ψ-quadrature vs. FV-MOL at matched resolution (the practical payoff — see Part 2).
9. Header docstring restates, by section reference, §3's five errata and §5's five verification findings — don't re-litigate them in the code, cite them.
10. Do not modify `mechanistic_verify.py`; import from it. If a Toth parameter is genuinely absent, use `mechanistic_verify.py`'s existing illustrative-flagged values and say so — per repo policy, name the missing parameter rather than silently guessing.

### 1.3 Acceptance criteria

- Task 3 (Danilov Fig.-1 reproduction) passes/fails is reported qualitatively (visual match to their published curve shape) — this is the OCR-reconstruction sanity check, not a project result.
- Task 4's three-way RMS deviation is reported as a number with no prescribed pass/fail threshold — that number is the open question Part 2 asks about, not a gate.
- Every item in §3 and §5 above is traceable to a specific line in the delivered code (comment, not just docstring preamble).

---

# PART 2 — RESEARCH DIRECTION

<research_direction>
```markdown
Don't search the internet. This is a test to see how well you can craft non-trivial, novel and creative proofs given a "Ψ-Quadrature Consistency and Error-Bound" math problem. Provide a full unconditional proof or disproof of the problem.

REMEMBER — this unconditional argument may require non-trivial, creative and novel elements.

### The problem

A packed-bed CO₂ adsorption column is governed by the coupled system (mass balance, LDF kinetics, nonlinear isotherm, energy balance) given in full below. Two independent routes predict its breakthrough behaviour in the constant-pattern regime.

**Route A — derived shape.** Assume $D_L=0$, isothermal, favorable isotherm $Q(c)$. The wave speed $v_{RH}=uc_f/[\varepsilon c_f+\alpha_bQ(c_f)]$ follows from a Rankine–Hugoniot jump condition (not assumed), and the wave shape $\varphi(\eta)$, $\eta=z-v_{RH}t$, follows from solving the ODE $\varphi'(\eta)=-\tfrac{kc_f}{v_{RH}q_f}[Q(\varphi)-\tfrac{q_f}{c_f}\varphi]$, which for Langmuir $Q$ integrates to $\ln w-(1+bc_f)\ln(1-w)=-\tfrac{kbc_f}{v_{RH}}(\eta-\eta_0)$, $w=\varphi/c_f$ — asymmetric, with distinct leading/tail e-folding rates whenever $bc_f\ne0$.

**Route B — assumed shape.** Define the driving force $\psi=q^*-q$. Under the chain-rule substitution $\partial y/\partial t=(\partial y/\partial q)(\partial q/\partial t)$ and two coefficients $\gamma_q,\beta$ that are $\approx1$ specifically for Langmuir isotherms (proved by direct evaluation, not assumed, for the reference system this method was validated on), $\psi$ satisfies the *linear* ODE $\partial\psi/\partial t=-\beta k\psi\Rightarrow\psi\propto e^{-\beta kt}$. A **symmetric two-branch exponential ansatz** is then stitched around the stoichiometric time $t_s$ in a shifted "corrected time" coordinate $\tau=(t-z/u_f)-t_s$ — symmetric by construction, regardless of $bc_f$ — and every field ($F,c,Q,T^G,q,T^S$) is reconstructed by marching Riemann-sum quadratures built on the exponential Green's-function kernel of the linear transport operator, seeded by that ansatz. No ODE for the *shape* is solved; the shape is posited and the bookkeeping is exact.

**Claim.** Set route B's free parameters equal to route A's derived quantities ($u_f\to v_{RH}$, $t_s\to t_{st}=L/v_{RH}$, isothermal, single-component Langmuir, $\gamma_q=\beta=1$). Then:

(a) *(Shared limit, easy direction — establish this first as a sanity check, not the main result.)* As $bc_f\to0$ (weak nonlinearity / Henry regime), both routes' predicted outlet curve converge to the same symmetric logistic $1/(1+e^{-kbc_f(t-t_{st})})$ — route A because its asymmetry vanishes ($\ell_+/\ell_-\to1$) as $bc_f\to0$, route B because its ansatz was symmetric all along and this is the regime where symmetry stops being a wrong assumption. Prove this shared limit rigorously (it should be a short, clean argument) and use it only to calibrate that both constructions are internally consistent before addressing (b)–(c), which are the substantial parts.

(b) *(Mesh-refinement convergence, away from the shared limit.)* Fix $bc_f=O(1)$ or larger (genuinely favorable/nonlinear). As route B's marching mesh is refined ($\Delta t,\Delta z\to0$), does its discrete profile converge to *route A's* exact closed-form profile $\varphi(\eta)$, to *its own* (wrong, symmetric) continuum ansatz limit, or to neither? If it converges to its own ansatz limit rather than to route A, quantify the resulting error floor — the gap between route B's converged-but-wrong-shape prediction and route A's true asymmetric profile — as an explicit, computable function of $bc_f$, and show whether that floor vanishes as $bc_f\to0$ (consistent with part (a)) and how it grows as $bc_f$ increases.

(c) *(Non-isothermal extension — where route A itself has no closed form, MM Part D.6.)* Restore Arrhenius coupling $b(T)$. Route A no longer has a closed-form comparator at all: the concentration and thermal waves generally travel at different speeds ($v_{RH}$ vs. $v_{th}=\gamma_hu$), producing two-front structures (documented numerically: an adiabatic run whose outlet stalls on a plateau at $c/c_f\approx0.955$ while $T$ remains $18.9\,$K above ambient, MM Appendix V.5) that a *single*-front ansatz cannot represent by construction, symmetric or not. Does route B's marching machinery (its heat-flux and temperature quadratures) still converge to *something* as the mesh refines, and if so, is there any sense in which that limit can be shown to bound, or fail to bound, the true two-front error — given that the only available comparator in this regime is not a closed form but the independently-validated finite-volume solution already in the repository (`mechanistic_verify.py`, reproduced below)? In particular, address whether route B's ansatz — built for a *single* front — can ever converge to a *two*-front solution under mesh refinement alone, or whether this is a structural limitation no amount of resolution fixes (i.e. whether the relevant error here is a discretisation error at all, or a model-class error that mesh refinement is powerless against).

Prove the claim in full generality for the system given, or disprove each part by exhibiting the specific mechanism that breaks it — for (b), identify which step in route B's "assume-then-march" construction is where the wrong-shape information gets baked in irrecoverably (is it Eq. 1's ansatz itself, or something introduced later in the marching quadratures of Eqs. 2–8 that could in principle correct an initially-wrong shape but doesn't); for (c), be explicit about whether "convergence" is even the right question to ask of a single-front scheme applied to a genuinely two-front problem, or whether the honest answer is that route B's applicability is bounded by a regime check (e.g. on $\Phi\cdot\delta_1$ or the front-speed ratio $v_{th}/v_{RH}$) rather than by a convergence rate.

Your answer must directly engage with:
- whether there is a general consistency/Lax-equivalence-type principle governing when a marching scheme built on an *assumed* (rather than *derived*) self-similar shape can still converge to the *true* solution, and if so what property of the ansatz and the marching operator that principle depends on;
- the fact that route B's own derivation (Appendix A/B of the source paper) proves $\gamma_q\approx1,\beta\approx1$ *specifically and only* for a Langmuir-type system at the operating point it was evaluated at — meaning the "$\approx1$" approximation route B relies on to even get a linear driving-force ODE is itself isotherm- and possibly operating-point-dependent, and any error-floor result for part (b) needs to state whether it is charging that error to the shape-ansatz mismatch, to the $\gamma_q,\beta\approx1$ approximation, or to genuine discretisation error, since conflating the three would make the "floor is a function of $bc_f$ alone" claim in part (b) untrustworthy;
- do not simply cite that convergence of marching/quadrature schemes to ODE solutions "is known" in general — construct or refute the argument from the specific governing equations and specific ansatz given below; generic finite-difference consistency theorems do not automatically transfer to a scheme built on an assumed rather than derived self-similar shape, and that gap is exactly what must be closed or exposed.

---

### Information you may or may not need

**Governing system (single-component CO₂, packed bed).**
$$\varepsilon c_t+uc_z=\varepsilon D_Lc_{zz}-\alpha_bq_t,\qquad q_t=k(q^*(c,T)-q),\qquad C_hT_t+u\rho_gc_{p,g}T_z=\lambda_{\mathrm{eff}}T_{zz}+\alpha_b(-\Delta H)q_t-\tfrac{4h_w}{d_{\mathrm{col}}}(T-T_{\mathrm{wall}}),$$
Toth closure $q^*(c,T)=n_s(T)b(T)c/[1+(b(T)c)^{t_T}]^{1/t_T}$, $b(T)=b_0\exp[\tfrac{\Delta H_0}{RT_0}(\tfrac{T_0}{T}-1)]$; Langmuir sub-case $Q(c)=q_mbc/(1+bc)$. Clean-bed IC, Danckwerts inlet, zero-gradient outlet.

**Route A closed forms.** $v_{RH}=uc_f/[\varepsilon c_f+\alpha_bQ(c_f)]$; $t_{st}=L/v_{RH}=\int_0^\infty(1-c(L,t)/c_f)dt$ (Corollary B.1, model-free, holds for *any* $D_L,k$ — usable as an independent check on route B's $t_s$ even outside the constant-pattern regime); Langmuir wave as above, tail rates $\ell_+^{-1}\propto Q'(0)-q_f/c_f$, $\ell_-^{-1}\propto q_f/c_f-Q'(c_f)$, equal only as $bc_f\to0$.

**Route B mechanics.** $\psi=q^*-q$; $\partial\psi/\partial t=-\beta k\psi$ (linear, from Appendix-B chain-rule substitution, valid where $\gamma_q,\beta\approx1$); two-branch exponential ansatz in corrected time $\tau=(t-z/u_f)-t_s$; Riemann-sum marching quadratures for flux/heat-flux/mole-fraction/gas-temperature; loading and solid temperature reconstructed algebraically; front kinematics $t_s=\tfrac{\varepsilon L}{u}(1+\Lambda)$, $\Lambda=\alpha_bq_f/(\varepsilon c_f)$ — identical, term for term, to route A's $t_{st}$ once $\Lambda$ is recognised as MM's own capacity ratio $\alpha$.

**Relaxation-expansion background (possibly relevant to part (b)'s order-of-convergence argument).** For large-but-finite $Da=kL/u$, a Chapman–Enskog expansion of the LDF ODE gives $q=Q(c)-k^{-1}\partial_tQ(c)+O(k^{-2})$, i.e. finite kinetics behaves as extra effective dispersion $D_{\mathrm{kin}}\propto v_{\mathrm{eff}}^2/k$ (a van Deemter C-term, derived not assumed) on top of $D_L$. This is an existing instance of "assume-equilibrium-then-correct-perturbatively" acquiring a systematic, computable error term in a small parameter — check whether route B's own small-parameter structure ($1-\gamma_q$, $1-\beta$, both $\to0$ specifically in the Langmuir limit) admits an analogous expansion, and whether the mesh-refinement limit and this small-parameter limit commute.

**Numerical ground truth for part (c) (no closed form exists here, per the governing model's own Part D.6).** An independently validated finite-volume Method-of-Lines solver (upwind convection, central dispersion, Danckwerts inlet as exact face flux) reproduces: the linear-isotherm analytical solution to $<1\%$ relative $L^2$ error; the Rankine–Hugoniot front speed to $0.020\%$ and the first-moment stoichiometric time to $<0.005\%$; the exact Langmuir travelling wave to $0.30\%$ RMS with zero fitted parameters; discrete mass-balance drift of $4.6\times10^{-5}$; and, for the full non-isothermal Toth case, a documented two-front structure (adiabatic run: outlet concentration plateaus at $c/c_f\approx0.955$ while outlet temperature remains $+18.9\,$K above ambient) that persists at fine mesh resolution — i.e. this is not itself a discretisation artefact to be refined away, it is the true solution's structure.

**Empirical corroboration (context, not proof-admissible evidence).** This project's own curve-fitting library, applied to five real packed-bed CO₂ breakthrough runs at 4.7–15.1% CO₂ on a PEI-functionalised sorbent, finds all five measured curves strongly right-skewed/asymmetric — fractal/asymmetric model families beat the symmetric logistic by roughly $6\times$ in RMSE. This is consistent with route A's prediction that asymmetry should be the norm once $bc_f$ is not small, and inconsistent with route B's ansatz being generally adequate — but the real runs are also non-isothermal, non-dilute, and finite-$Pe$, so this observation cannot be used as evidence inside the proof itself; it only motivates why the question is worth answering rigorously rather than dismissing route B outright.
```
</research_direction>

---

## Notes for the lead researcher (not part of either sent prompt)

- **Why this direction and not another.** `prompts/prompt00.md` already poses one open "prove or disprove" problem for this project (non-isothermal constant-pattern uniqueness). This one is deliberately a different question — a *scheme-comparison* question (does an assumed-shape marching quadrature converge to a derived-shape closed form, and where does it stop helping) rather than a *well-posedness* question — chosen specifically because §§1–5 above already establish the two constructions share a boundary condition, a front-kinematics formula, and a weak-nonlinearity limit, which is exactly the scaffolding a rigorous comparison needs and a from-scratch pairing wouldn't have.
- **Timeline fit.** `CLAUDE.md`'s Gates A–C were due by end of Wk 6 / Interim Report (1 Jun 2026), which has passed as of this session (8 Jul 2026). This is framed as a **post-gate, Final-Report-facing contribution** (due 10 Aug 2026): it builds on already-validated machinery (`mechanistic_verify.py`, Gates A/B/D.8 all passing per Appendix V) rather than requiring new experimental data or the still-open `??` Toth parameters.
- **Practical payoff if part (b) or (c) is resolved.** A validated error floor as a function of $bc_f$ (or a regime check replacing it, per (c)'s framing) would justify *when* the cheap $O(N)$-per-timestep ψ-quadrature is an adequate stand-in for the stiffer FV-MOL solve — useful for the parametric sweep matrix `CLAUDE.md` flags as still needing to be executed, where a cheaper bounded-error surrogate earns its keep across many $(u,c_f,L,T)$ points.
- **Suggested order.** Run Part 1's implementation first — it produces Task 3's Danilov-Fig.-1 reproduction (an OCR-correctness sanity check) and Task 4's three-way RMS numbers, both of which Part 2's proof should be checked against, not derived in a vacuum. Then run Part 2 twice — once for a creative disproof attempt, once for a rigor-focused proof attempt — and diff the two before trusting either, following the pattern already established in `prompt00.md`'s own closing note.